# Для этого проекта переменные окружения во фронтенде не нужны.
#
# Заявка — через Яндекс.Форму (ID вписывается в src/components/LeadForm.vue).
# Объявления из Telegram — функция api/telegram.js берёт имя канала из
# переменной TG_CHANNEL, которая задаётся в настройках проекта на Vercel
# (Environment Variables), а не в этом файле.
